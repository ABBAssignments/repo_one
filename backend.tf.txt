# backend.tf

terraform {
  backend "azurerm" {
    resource_group_name  = var.resource_group_name
    storage_account_name = "mytfstatestorage"  # Update to a unique name
    container_name       = "terraform-state"
    key                  = "terraform.tfstate"
  }
}
